# SCC Core (Foundation Package)

Foundation package providing cluster manifest schemas, values loader, Jinja2 templating engine, lifecycle hook manager, atomic locking, and base provider abstractions.
